package com.jphuey.gameflix.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Optional: configure static resources, interceptors, formatters if needed in the future
}